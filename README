Some jupiter icons which work great with the ubuntu mono themes.

The "dark" (read: nearly white) icons should be used with the dark icon theme (ubuntu-mono-dark) and vice-versa.

These files should be placed in /usr/share/pixmaps/jupiter/

:)
